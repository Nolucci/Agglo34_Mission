<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ titre }}</title>
    <style>
        @page {
            size: portrait;
            margin: 10mm;
        }
        body {
            font-family: Arial, sans-serif;
            margin: 10mm; /* Réduire les marges */
            font-size: 10px;
        }
        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 15px; /* Réduire la marge */
            font-size: 18px; /* Réduire la taille du titre */
        }
        .info {
            text-align: right;
            margin-bottom: 15px; /* Réduire la marge */
            font-size: 10px; /* Réduire la taille de la police */
            color: #666;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px; /* Réduire la marge */
            table-layout: fixed; /* Fixer la disposition du tableau */
        }
        th, td {
            border: 1px solid #ddd;
            padding: 5px; /* Réduire le padding */
            text-align: left;
            word-wrap: break-word; /* Permettre le retour à la ligne */
            font-size: 9px; /* Réduire la taille de la police dans le tableau */
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .badge-success {
            background-color: #28a745;
            color: white;
            padding: 3px 6px;
            border-radius: 3px;
            font-size: 12px;
        }
        .badge-secondary {
            background-color: #6c757d;
            color: white;
            padding: 3px 6px;
            border-radius: 3px;
            font-size: 12px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <h1>{{ titre }}</h1>

    <div class="info">
        <p>Date d'export: {{ date_export|date('d/m/Y H:i:s', 'Europe/Paris') }}</p>
        <p>Nombre de boxs: {{ boxes|length }}</p>
    </div>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Commune</th>
                <th>Service</th>
                <th>Adresse</th>
                <th>Ligne Support</th>
                <th>Type</th>
                <th>Attribué à</th>
                <th>Statut</th>
            </tr>
        </thead>
        <tbody>
            {% for box in boxes %}
                <tr>
                    <td>{{ box.id }}</td>
                    <td>{{ box.commune ? box.commune.name : 'Non défini' }}</td>
                    <td>{{ box.service ?: 'Non défini' }}</td>
                    <td>{{ box.adresse ?: 'Non défini' }}</td>
                    <td>{{ box.ligneSupport ?: 'Non défini' }}</td>
                    <td>{{ box.type ?: 'Non défini' }}</td>
                    <td>{{ box.attribueA ?: 'Non défini' }}</td>
                    <td>
                        {% if box.statut == 'Actif' %}
                            <span class="badge-success">Actif</span>
                        {% else %}
                            <span class="badge-secondary">Inactif</span>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        <p>Document généré automatiquement - Agglo34</p>
    </div>

    <script>
        window.onload = function() {
            // Attendre que la page soit chargée pour imprimer
            window.print();
        };
    </script>
</body>
</html>